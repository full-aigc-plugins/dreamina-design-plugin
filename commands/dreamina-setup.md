---
description: 即梦首次配置：CLI 安装、登录鉴权与状态预检
argument-hint: "[要配置或诊断的项：login|account|cli]"
---

按 vendored `dreamina-cli` 技能完成首次配置与状态预检：

1. CLI 安装/更新（未装或过期时按技能指引处理）。
2. 登录鉴权（`dreamina cloud login` / 会话态检查），确认账户可用。
3. 预检汇报：CLI 版本、登录态、账户、默认出片参数——有异常按技能的排障段处理。

配置完成后可直接使用 `/dreamina-text2image` 等生成命令。
